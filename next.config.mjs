/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Special-Birthday-Gift', // Aapki repository ka exact naam
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
